import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ar.agentech.inmobiliariasnea',
  appName: 'Inmobiliarias NEA',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
